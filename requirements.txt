# uv가 없을 때 pip 폴백용:
#   pip install -r requirements.txt
# 테스트 실행 시 src를 import 경로에 추가해야 한다:
#   PYTHONPATH=src pytest
pydantic>=2.6
pyyaml>=6.0
typer>=0.12
rich>=13.0
matplotlib>=3.8
streamlit>=1.36
pytest>=8.0
