# 데이터 작성 가이드 — "왜 이렇게 쓰나" (비전공 신입용)

이 문서는 **코딩도 화학도 처음인 사람**이, YAML 파일이 왜 이렇게 생겼는지 이해하고 **직접 고쳐서 테스트**까지 할 수 있게 쓴 가이드입니다.
명령어 사용법은 [`GUIDE.md`](GUIDE.md), 한 제품을 끝까지 따라가는 예시는 [`예시_시나리오.md`](예시_시나리오.md)를 보세요.

---

## 0. 큰 그림: 이 도구는 "데이터"와 "계산기"가 분리되어 있다

이 도구는 두 부분입니다.

- **데이터** = 여러분이 편집하는 YAML 파일들 (`data/`, `formulas/` 폴더). 원료·처방·가격·규제 숫자.
- **계산기** = 파이썬 코드 (`src/` 폴더). 여러분은 **건드리지 않습니다.**

원료 가격을 바꾸고 싶으면? 코드가 아니라 **YAML 숫자 한 줄만** 바꿉니다. 그러면 원가·손익 계산이 전부 자동으로 바뀝니다.
👉 그래서 비전공자도 **데이터만 이해하면** 이 도구를 씁니다. 이 문서는 그 "데이터"를 다룹니다.

### 왜 하필 YAML인가? (엑셀·DB가 아니라)

| 이유 | 설명 |
|---|---|
| **사람이 읽는다** | 메모장으로 열어 그냥 읽힙니다. 특수 프로그램이 필요 없어요. |
| **버전 관리(Git)** | "어제와 오늘 뭐가 바뀌었나"가 한 줄 단위로 남습니다. 처방은 소중한 기록이니까요. |
| **코드와 분리** | 값만 바꾸면 계산이 바뀝니다. 프로그래머가 필요 없어요. |
| **실수를 잡아준다** | 형식이 틀리면 도구가 **어디가 틀렸는지 알려주고 멈춥니다**(조용히 넘어가지 않음). |

---

## 1. YAML 5분 문법 (이것만 알면 됩니다)

YAML은 "이름: 값"을 적는 아주 단순한 형식입니다. 딱 5가지만 기억하세요.

### ① 이름과 값은 콜론(`:`)으로
```yaml
brand_name: 라벤더랩
default_batch_g: 100
```
> 콜론 뒤에 **띄어쓰기 한 칸** 필수. `brand_name:라벤더랩` (X)

### ② 들여쓰기 = 소속 관계 (★ 제일 중요, 제일 많이 틀림)
들여쓰기(앞 공백)로 "누구 밑에 속하는지"를 표현합니다. **공백 2칸**을 쓰고, **탭(Tab) 금지**입니다.
```yaml
economics:            # 이 아래 들여쓴 것들은 economics의 하위 항목
  channel_fee_rate: 0.30
  shipping_cost: 3000
```
> 탭을 쓰거나 칸 수가 안 맞으면 에러가 납니다. 편집기에서 "탭을 공백으로" 설정을 켜세요.

### ③ 목록은 대시(`-`)로
여러 개를 나열할 땐 `-`로 시작합니다.
```yaml
minimal_items:
  - 화장품의 명칭
  - 가격
  - 제조번호
```

### ④ 한 줄에 몰아 쓰는 축약형 `{ }`
목록의 각 항목이 짧으면 중괄호로 한 줄에 씁니다. 아래 두 개는 **완전히 같은 뜻**입니다.
```yaml
# 풀어 쓴 형태
ingredients:
  - id: mct
    percent: 40.0
# 축약형 (같은 뜻)
ingredients:
  - { id: mct, percent: 40.0 }
```
> ⚠️ 축약형 `{ }` 안의 값에 **쉼표(,)가 들어가면** 도구가 다음 항목으로 착각합니다. 그럴 땐 값을 큰따옴표로 감싸세요:
> `{ 시점: 30분, 메모: "제라늄, 로지-그린" }`

### ⑤ 주석은 `#`, 따옴표는 헷갈릴 때만
```yaml
price_per_kg: 15000        # 이건 주석 (도구가 무시함)
cas: "73398-61-5"          # 숫자처럼 보이지만 문자라서 따옴표 (안전)
```
> 대부분 따옴표 없이 됩니다. **숫자로 오해될 수 있는 문자**(CAS 번호, `45C` 같은 코드)나 **쉼표·콜론이 든 문장**만 따옴표로 감싸면 됩니다.

### 편집 → 확인 루프
YAML을 고친 뒤에는 **항상** 도구로 확인합니다. (아래 각 파일 설명에 확인 명령이 있습니다.)
```bash
uv run python -c "from brandlab.loader import BrandLab; BrandLab.load(); print('전체 로드 OK')"
```
에러가 나면 겁먹지 마세요 — 도구가 **어느 파일 몇 번째가 왜 틀렸는지** 알려줍니다. 3장에서 에러 읽는 법을 설명합니다.

---

## 2. 파일별 "왜 이렇게 생겼나"

각 파일마다 **무엇인지 → 최소 예시 → 필드마다 [무엇/왜/틀리면] → 흔한 실수** 순서로 설명합니다.

---

### 2.1 `data/ingredients.yaml` — 원료 사전(주소록)

**무엇:** 내가 쓰는 모든 원료의 "마스터 정보"를 한 곳에 모은 사전입니다. 처방은 여기 있는 원료를 **id로 불러서** 씁니다.

**왜 이렇게?** 원료 정보를 처방마다 반복해서 쓰면, 가격이 바뀔 때 처방을 다 고쳐야 합니다. 대신 **여기 한 곳에만** 적고 처방은 id로 참조하면, 가격을 여기서 한 번 바꾸면 모든 처방·원가가 자동 반영됩니다. (주소록에 연락처를 한 번 저장하고, 여기저기서 이름으로 부르는 것과 같아요.)

**최소 예시 (필수 4개만):**
```yaml
ingredients:
  - id: my-oil
    name: 내오일
    inci: My Oil
    category: 에몰리언트
```

**필드 설명:**

| 필드 | 필수? | 무엇 / 왜 | 틀리면 |
|---|---|---|---|
| `id` | ✅ | 처방이 부를 **이름표**. 영문·하이픈, 공백 없이(예: `shea-butter`). | 처방에서 이 id를 못 찾으면 에러 |
| `name` | ✅ | 한글 표시명(사람용). | — |
| `inci` | ✅ | **INCI**(국제 표준 성분명). 라벨(전성분)에 그대로 찍히는 **법적 이름**. | 라벨이 틀리게 나옴 |
| `category` | ✅ | 기능 분류(에몰리언트/왁스/계면활성제/버터 등). **스케일업 리스크·조향 계산에 쓰임**(왁스·버터 합계, 유화제 유무). | 리스크 판정이 틀림 |
| `price_per_kg` | | kg당 원(원가 계산용). | 없으면 원가 0으로 잡고 경고 |
| `density` | | 밀도(g/㎖). 액체 제품에서 **부피→무게 환산**에 필요. | 없으면 1.0으로 가정 |
| `has_coa` | | 성적서(품질 증명) 보유 여부. **기본값 false**(=미확인). | false면 원료 화면에서 **빨갛게** 표시 |
| `cosmetic_grade` | | 화장품용 등급인가? **기본값 true**. 캔들·공업용이면 false. | false면 빨갛게 표시(실수 방지) |
| `cas` | | CAS 번호(성분 고유 번호). 숫자처럼 보여 따옴표 권장. | — |
| `allergens` | | 이 원료에 든 알레르기 성분과 함량 → `- { id: linalool, percent: 30.0 }` | 라벨 알러젠 표기가 누락됨 |
| `fragrance` / `colorant` | | 착향제/착색제 여부. 전성분 표시 순서 규칙에 쓰임. | — |

**`has_coa` 기본값이 왜 false인가?** 안전한 쪽으로 기본을 잡았습니다. "성적서 있다"고 증명되기 전엔 "없음(확인 필요)"으로 보고 빨갛게 경고합니다. 성적서를 확보하면 `has_coa: true`로 바꾸세요. **"캔들용 향료를 화장품에 쓰는 실수"**를 막는 게 이 필드의 목적입니다.

**`allergens`를 왜 `{id, percent}`로?** 완제품에 알러젠이 얼마나 들었는지 계산하려면 두 숫자가 필요합니다: "처방에 이 향료를 몇 % 넣나" × "그 향료 안에 알러젠이 몇 %인가". 예: 향료를 0.3% 넣고 그 안에 리모넨이 5%면 → 완제품엔 0.3 × 5% = **0.015%**. 그래서 원료마다 알러젠 함량(percent)을 적어둡니다.

**확인:**
```bash
uv run python -c "from brandlab.loader import load_ingredients as L; print('my-oil' in L().index())"
```

---

### 2.2 `formulas/<슬러그>/v<번호>.yaml` — 처방(레시피 카드) ★ 가장 중요

**무엇:** 제품 하나의 레시피입니다. 예: `formulas/shampoo-bar/v1.yaml`.

**폴더·파일 이름 규칙(왜 중요):**
- 폴더 이름 = **슬러그**(제품을 부르는 이름, 예: `shampoo-bar`). 명령에서 이 이름을 씁니다.
- 파일 이름 = `v` + 버전번호(예: `v1.yaml`, `v2.yaml`). 처방을 고칠 땐 파일을 **지우지 말고** `v2.yaml`을 새로 만들어 이력을 남깁니다.

**최소 예시:**
```yaml
product: 라벤더 고체 샴푸바        # 사람이 읽는 제품명
slug: shampoo-bar                # 폴더 이름과 같게
version: 1
regime: cosmetics                # 적용 법(레짐)
product_type: rinse_off          # 씻어냄/안 씻어냄
status: 개발중
base_batch_g: 300
phases:
  - name: A
    ingredients:
      - { id: sci, percent: 55.0 }
      - { id: water, percent: 45.0 }
```

**필드 설명:**

| 필드 | 필수? | 무엇 / 왜 |
|---|---|---|
| `product` | ✅ | 제품명(한글, 사람용). |
| `slug` | ✅ | 명령에서 부르는 이름. **폴더명과 같게.** |
| `version` | ✅ | 버전(1 이상 정수). |
| `regime` | | 적용 법. `cosmetics`(화장품)/`chemical_safety`(생활화학)/… **기본값 cosmetics.** 이 값이 라벨·비용 규칙을 결정. |
| `product_type` | ✅ | `leave_on`(안 씻어냄: 크림·오일) / `rinse_off`(씻어냄: 샴푸·클렌저). **알러젠 표기 기준을 정함.** |
| `status` | ✅ | `개발중` / `확정`. |
| `base_batch_g` | ✅ | 기준 배치 용량(g). |
| `phases` | ✅ | **상(相) 목록** — 아래 설명. |
| `packaging` | | 쓰는 용기 → `- { id: soap-box, qty_per_unit: 1 }` |
| `fill_volume_ml` | | 1개 충전량(㎖). 액체 제품 표시의무 판정에 사용. |
| `net_weight_g` | | 1개 내용량(g). 고체(비누·샴푸바)는 부피 대신 이걸로 판정. |
| `parent_version` | | 이 버전이 어느 버전에서 파생됐는지. |

**왜 "상(phase/A·B·C)"으로 나누나?** 이건 화장품 이론이 아니라 **실제 제조 순서**를 그대로 담은 겁니다. 예를 들어:
- A상: 오일·왁스를 **가열해 녹임**
- B상: 물·수용성 원료를 따로 준비
- C상: 열에 약한 향·비타민을 **식힌 뒤 마지막에** 투입

배치 지시서가 이 상 순서대로 "무엇을 언제 넣는지" 인쇄해 주기 때문에, 상을 나누면 **작업 순서가 그대로 지시서가 됩니다.** 각 상에 `process:` 한 줄로 공정 설명을 적을 수 있습니다.

**왜 그램(g)이 아니라 퍼센트(%)로 적나?** 레시피는 **비율**입니다. 비율로 적어두면 100g을 만들든 5kg을 만들든 도구가 그때그때 g으로 환산해 줍니다(`batch` 명령). 그래서 처방엔 %만, 배치 크기는 명령에서 지정합니다.

**왜 합계가 꼭 100이어야 하나?** %는 "전체 중 몇 %"라는 뜻입니다. 합이 100이 아니면 그건 **완성된 레시피가 아닙니다**(빠뜨렸거나 계산 실수). 도구는 **추측하지 않고** 멈춰서 알려줍니다:
```
처방 percent 합계가 100 ± 0.01이 아닙니다: 합계 90.0000%
```
→ 빠진 원료(보통 물)를 넣어 100을 맞추세요. (±0.01은 반올림 오차 허용치)

**확인:**
```bash
uv run brandlab label shampoo-bar v1     # 라벨이 나오면 처방이 유효
uv run pytest tests/test_references.py -q # 참조·합계 자동 검사
```

---

### 2.3 `data/packaging.yaml` — 포장재 사전

**무엇:** 용기·박스 정보. 처방의 `packaging`이 여기 id를 참조합니다.

```yaml
packaging:
  - id: soap-box
    name: 비누 종이 박스
    type: box
    unit_price: 300       # 개당 원(원가 계산)
    moq: 1000             # 최소 주문 수량
```

| 필드 | 필수? | 무엇 / 왜 |
|---|---|---|
| `id`,`name`,`type` | ✅ | 이름표 / 한글명 / 종류(jar·tube·box…) |
| `unit_price` | | 개당 단가(원가에 더해짐) |
| `moq` | | **최소 주문 수량**. 아래 "왜 중요" 참고 |
| `volume_ml`,`material`,`supplier` | | 용량/재질/공급처 |

**`moq`가 왜 중요한가?** 용기는 "최소 몇 개부터" 주문됩니다(보통 2,000~3,000개). 1,000개만 만들 건데 MOQ가 3,000이면 **2,000개가 남아 창고에 돈이 묶입니다(사장 재고).** `cost` 명령이 이걸 계산해 "선투입 자본"으로 보여줍니다 — 1인 브랜드에서 현금이 묶이는 실제 지점이라 꼭 넣습니다.

---

### 2.4 `data/config.yaml` — 브랜드 설정(손익 가정)

**무엇:** 원가·손익 계산에 필요한 "가정값"을 모아둔 곳입니다.

```yaml
brand_name: 라벤더랩
economics:
  channel_fee_rate: 0.30   # 판매 채널 수수료 30% (0.30 = 30%)
  shipping_cost: 3000      # 개당 배송비
  return_rate: 0.05        # 반품률 5%
  vat_rate: 0.10           # 부가세 10%
  target_margin: 0.40      # 목표 마진 40%
regulatory_thresholds:
  high_entry_cost: 1000000 # 진입비용 100만원 넘으면 경고
  long_lead_time_days: 30  # 시험기간 30일 넘으면 경고
```

**왜 코드가 아니라 여기에?** 수수료율·배송비는 사람마다·시기마다 다릅니다. 코드에 박아두면 프로그래머가 고쳐야 하지만, 여기 숫자를 바꾸면 손익이 즉시 다시 계산됩니다. `cost` 결과 맨 아래에 "이 값들은 config.yaml에서 왔다"고 **출처를 밝혀** 줍니다(가정이 결과를 좌우하니까요).
> 비율은 소수로 씁니다: 30% → `0.30`, 5% → `0.05`.

---

### 2.5 `data/regulatory/**` — 규제 숫자 (법령 값)

**무엇:** 알러젠 기준·배합한도·표시규정·광고 금지표현·시험비 같은 **법에서 정한 숫자**들. 레짐(법)별로 폴더가 나뉩니다: `cosmetics/`(화장품), `chemical_safety/`(생활화학).

**왜 코드에 안 넣고 파일로?** ① **법은 바뀝니다.** 파일 값과 날짜(`last_updated`)만 고치면 되고, 프로그래머가 필요 없습니다. ② 각 파일에 `source_url`(출처)과 `last_updated`(갱신일)를 두어 **어디서 온 값인지, 얼마나 오래됐는지** 추적합니다. **180일 지나면 도구가 경고**합니다(법 개정을 놓치지 않으려고).

비전공자가 자주 만질 파일은 하나입니다:
- **`cosmetics/ad_terms.yaml`** — 상세페이지에서 걸러낼 금지·주의 표현. 초기엔 예시 10개뿐이라 **직접 추가**해야 합니다.
```yaml
  - expression: 미백              # 걸러낼 표현(기본형)
    category: functional_claim   # 분류
    risk: high                   # 위험도 high/medium/low
    suggestion: "피부 톤 케어"    # 대체 표현
    reference: "기능성 심사 대상"
```
> `expression`은 기본형만 적으면 "미백효과", "미백에"처럼 변형도 자동으로 잡힙니다(정규식 처리). 여러 단어는 띄어 쓰면(`주름 개선`) 붙여 쓴 것("주름개선")도 잡습니다.

나머지 규제 파일(알러젠·배합한도·시험비)은 값 자체를 고칠 일이 있을 때만 열고, **고치면 반드시 `last_updated`도 오늘 날짜로** 바꾸세요.

---

### 2.6 조향 파일 (향수·디퓨저를 만들 때만)

- **`data/aroma_materials.yaml`** — 향료 원료. `노트`(top/middle/base), `계열`, `ifra_한도_퍼센트`(안전 한도) 등.
- **`formulas/fragrance/<이름>-v<n>.yaml`** — 향 처방. 여기 `parts`는 **원액 기준 비율**, `dilution`은 **계량용 희석 농도**입니다.

**왜 `parts`와 `dilution`을 나누나?** 조향은 원액을 그대로 못 넣고 보통 **희석액**으로 계량합니다. `parts`(원액 비율)와 `dilution`(예: 10%)을 함께 적으면, 도구가 "원액 1.5g을 넣으려면 10% 희석액 15g을 계량하라"처럼 자동 환산해 줍니다.
> 파일 이름은 `v`로 시작하면 안 됩니다(일반 처방과 구분). `citrus-cologne-v1.yaml`처럼 이름을 앞에 두세요.

---

## 3. 에러 메시지 읽는 법 (겁먹지 마세요)

도구는 틀리면 **어디가 왜 틀렸는지** 알려주고 멈춥니다. 자주 보는 것들:

| 에러 문구(일부) | 뜻 | 해결 |
|---|---|---|
| `처방 percent 합계가 100 … 아닙니다: 합계 90.0` | 재료 % 합이 100이 아님 | 빠진 재료(보통 물)를 넣어 100 맞추기 |
| `참조하는 원료 id가 ingredients.yaml에 없습니다: ['nonexistent']` | 처방이 부른 원료가 사전에 없음 | id 철자 확인, 또는 그 원료를 `ingredients.yaml`에 추가 |
| `Input should be 'leave_on' or 'rinse_off'` | `product_type`에 엉뚱한 값 | `leave_on` 또는 `rinse_off`로 |
| `Field required` | 필수 필드 빠짐 | 어떤 필드인지 함께 나옴 → 채우기 |
| `Extra inputs are not permitted` | 오타 필드거나, `{ }` 안 값에 쉼표가 들어감 | 필드명 확인, 쉼표 든 값은 `"따옴표"`로 |
| `mapping values are not allowed here` / `could not find expected ':'` | YAML 들여쓰기·콜론 문제 | 공백 2칸 확인, 탭 금지, `키: 값`의 콜론 뒤 띄어쓰기 |

**팁:** 에러에 나오는 숫자(예: 합계 90.0)를 그대로 읽으면 대부분 원인이 보입니다.

---

## 4. 따라하기: 내 첫 원료·처방 추가하고 테스트 (복붙 가능)

목표: "내오일"이라는 원료를 만들고, 그걸로 아주 단순한 오일 제품을 만들어 라벨까지 확인.

**1) 원료 추가** — `data/ingredients.yaml` 맨 아래 `ingredients:` 목록에 붙이기:
```yaml
  - id: my-oil
    name: 내오일
    inci: My Test Oil
    category: 에몰리언트
    price_per_kg: 10000
    density: 0.9
    has_coa: true
    grade: cosmetic
```

**2) 처방 폴더·파일 만들기** — `formulas/my-oil-serum/v1.yaml`:
```yaml
product: 내 오일 세럼
slug: my-oil-serum
version: 1
regime: cosmetics
product_type: leave_on
status: 개발중
base_batch_g: 100
phases:
  - name: A
    process: 상온에서 섞기
    ingredients:
      - { id: my-oil, percent: 99.5 }
      - { id: tocopherol, percent: 0.5 }
fill_volume_ml: 30
```

**3) 로드되는지 확인:**
```bash
uv run python -c "from brandlab.loader import BrandLab; BrandLab.load(); print('OK')"
```
에러가 나면 3장 표에서 문구를 찾아 고치세요.

**4) 기능 실행:**
```bash
uv run brandlab batch my-oil-serum v1 --grams 500   # 500g으로 환산
uv run brandlab label my-oil-serum v1               # 전성분·표시의무
```

**5) 자동 테스트로 검증:**
```bash
uv run pytest tests/test_references.py tests/test_masters.py -q   # 참조·로드 검사
uv run pytest -q                                                  # 전체(회귀 확인)
```
`passed`가 나오면 성공입니다. (연습이 끝나면 만든 폴더/원료를 지워도 됩니다.)

---

## 5. 도메인 용어 아주 쉽게

| 용어 | 쉬운 설명 |
|---|---|
| **INCI** | 성분의 국제 표준 이름. 라벨 전성분에 찍히는 공식 명칭. |
| **CAS 번호** | 성분마다 붙은 고유 등록번호(주민번호 같은 것). |
| **상(phase, A/B/C)** | 제조할 때 함께 넣고 처리하는 재료 묶음. 실제 작업 순서. |
| **percent(w/w)** | 무게 기준 비율. 합이 100 = 완성된 레시피. |
| **leave_on / rinse_off** | 안 씻어냄(크림·오일) / 씻어냄(샴푸·클렌저). 알러젠 표기 기준이 다름. |
| **알러젠** | 알레르기를 일으킬 수 있어 일정 농도 넘으면 라벨에 따로 적어야 하는 성분(향료 26종 등). |
| **CoA** | 성적서. 원료 품질을 증명하는 서류. |
| **MOQ** | 최소 주문 수량. 이것 때문에 남는 재고(사장 재고)가 생김. |
| **레짐(regime)** | 제품에 적용되는 법. 화장품법이냐 생활화학제품법이냐에 따라 비용·라벨이 완전히 다름. |
| **배합한도** | 특정 원료를 넣을 수 있는 법적 최대 %. |
| **IFRA 한도** | 향료 성분의 안전 사용 한도. |

---

## 6. 절대 잊지 말 것

- 이 도구의 라벨·규정·판정은 **1차 스크리닝**입니다. "통과 = 합법"이 아닙니다.
- 저장소의 단가·알러젠%·IFRA한도·시험비 등은 **예시값**입니다. 실제로 쓰기 전 실제 값으로 바꾸세요([`사용_참고사항.md`](사용_참고사항.md) 참고).
- 규제 값을 고치면 같은 파일의 `last_updated`도 갱신하세요.
- 출시·신고 전 **식약처·환경부(KTR)·관할 기관에 확인**하세요.
